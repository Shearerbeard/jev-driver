# TODO: run `cargo deny check` once cargo-deny is installed and settle the
# license allow-list against the real dependency tree (reqwest -> native-tls
# -> openssl-sys pulls in the OpenSSL license, webpki roots, etc.).
[graph]
all-features = true

[licenses]
version = 2
allow = [
    "MIT",
    "Apache-2.0",
    "MIT-0",
    "ISC",
    "Unicode-3.0",
    "BSD-3-Clause",
    "BSD-2-Clause",
    "0BSD",
    "Zlib",
    "CDLA-Permissive-2.0",
    "MPL-2.0",
]
